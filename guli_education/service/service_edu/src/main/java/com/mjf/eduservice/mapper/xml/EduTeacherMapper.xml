<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mjf.eduservice.mapper.EduTeacherMapper">

    <select id="getAllTeacher" parameterType="JSONObject" resultType="JSONObject">
        SELECT *
        FROM edu_teacher
<!--        WHERE name LIKE #{teacher_name}-->
<!--        AND gmt_create >= #{start_date}-->
<!--        AND gmt_create <= #{end_date}-->
    </select>
</mapper>
